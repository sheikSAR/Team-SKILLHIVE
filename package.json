{
  "name": "edublocks-app",
  "version": "1.0.0",
  "description": "Edublocks - A blockchain-based educational platform",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd client && npm start\" \"cd server && npm start\"",
    "server": "cd server && npm start",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
